# PulseTrade.Comm.Spa.Dynamic WBS

Progress 是粗略 implementation checkpoint，不代表正式驗收。

| ID | 工項 | 狀態 | Progress | Test ID | 證據 / 備註 |
|---|---|---:|---:|---|---|
| WBS-001 | Harness 與文件鏈初始化 | Done | 100 | - | `doc/` 已有 REQ/SA/SD/WBS/TEST/UPSTREAM_RFC，目錄與專案結構建置完成。 |
| WBS-002 | 單元測試框架與殼層 | Done | 100 | - | `tests/PulseTrade.Comm.Spa.Dynamic.Tests.fsproj` 建立，引入 Expecto，並在 `Program.fs` 中建立 TDD 測試殼層。 |
| WBS-003 | 核心模組與依賴參考 | Done | 100 | - | `src/PulseTrade.Comm.Spa.Dynamic.fsproj` 建立，加入 `PulseTrade.Comm.Spa 0.2.4-beta7` 與 `FAkka.FCell2 10.1.301`。 |
| WBS-101 | 後端擴充點掛載測試 | Done | 100 | TEST-101 | 驗證 `CommHub.useDynamicSdui()` 能正確註冊 Actor 而不拋出例外。(Pending upstream) |
| WBS-102 | FCell AST Parser 轉換測試 | Done | 100 | TEST-102 | 驗證 `FCell2Interop.toJsonString` 能將 `fCell2` AST 正確輸出含有 `fskynet-sdui` 的 JSON。 |
| WBS-103 | 前端 Renderer 註冊與攔截測試 | Done | 100 | TEST-103 | 驗證 WebSharper 前端 `DynamicSduiRenderer` 能夠攔截包含 `fskynet-sdui` 標籤的字串。(Pending upstream) |
| WBS-104 | "Actor Dynamic" Tab Page 註冊測試 | Done | 100 | TEST-104 | 驗證前端能成功註冊並呼叫 `actor-dynamic` 頁面類型的 DOM 生成。 |
| WBS-201 | 實作 FCell2Interop (AST轉JSON) | Done | 100 | TEST-102 | 將原 POC 中的 FCell 轉 JSON 邏輯抽出為通用 library function。 |
| WBS-202 | 實作 ShowcaseDemoActor | Done | 100 | - | 將動態 Demo 的 Actor 狀態機與訊息處理從 POC 移出。 |
| WBS-203 | 實作 CommHub 擴充方法 | Done | 100 | TEST-101 | 實作 `.useDynamicSdui()`，將 Actor 與系統綁定。 |
| WBS-204 | 實作 Client/DynamicRenderer | Done | 100 | TEST-103 | 實作解析 DOM、Canvas 按鈕彈出邏輯，取代原本 PTCS 內的 hardcoded function。 |
| WBS-205 | 實作 Actor Dynamic Tab Page | Done | 100 | TEST-104 | 根據 SD 設計，實作前端的 Tab 頁面容器與對應的 SDUI 展示元件掛載。 |
| WBS-301 | 測試覆蓋率與 TDD 驗收 | Done | 100 | ALL | 確保 Expecto 中所有 WBS-100 系列測項由 Failing 轉為 Passing。 |
| WBS-302 | NuGet Pack 與發佈設定 | Done | 100 | - | 準備 `.nuspec` 或 MSBuild pack 參數，輸出 `0.1.0-alpha1`。 |
| DYN-WBS-401 | Dynamic Argu Form formal RFC flow | Review | 100 | DYN-T-401 | 新增 `RFC-PTCS-DYNAMIC-0002.dynamic-argu-form-runtime.md`，並同步 REQ/SA/SD/WBS/TEST/Traceability/DevLog；來源草稿保留為 `REQ_Dynamic_Argu_Form.md` / `RFC_Dynamic_Argu_Form.md`。 |
| DYN-WBS-402 | Argu metadata registry / schema generator | Review | 100 | DYN-T-402 | 新增 `Server/ArguForm.fs`：allowlisted sample DU schema、`schema=fskynet-sdui`、`formMode=argu-form`、text/number/enum/tuple/list/bool metadata；Expecto `DYN-T-402` 通過。Remaining：若後續開放 arbitrary DU reflection，需另補安全 allowlist gate。 |
| DYN-WBS-403 | SubmitArguForm state and command-line codec | Review | 100 | DYN-T-403 | 新增 `SubmitArguFormCodec.buildRawArgu` 並以 Expecto `DYN-T-403` 驗證 whitespace/quote escaping、list repetition、bool flag；browser renderer 也以同語意更新 raw preview。 |
| DYN-WBS-404 | Append input renderer integration | Review | 95 | DYN-T-404 | 新增 `Client/ArguFormRenderer.fs`，透過 PTCS `PulseTradeRegisterAppendInputRenderer` 註冊 `dynamic-argu-append-input`；selected Dynamic key 時取代 textarea，並在 E2E 驗證 text/number/enum/tuple/bool/list controls、raw preview、submit、append renderer throw fallback、blank renderer submit controlled validation 與 desktop/mobile geometry。Remaining：後續若開放更多 payload shape，再補專門 invalid-node unit gate。 |
| DYN-WBS-405 | Add-key dialog renderer integration | Review | 96 | DYN-T-405 | Argu-first first slice 已驗證 Dynamic add-key renderer 可回傳 variable-length key 並保留 union cases as list tail。`1:duType:` / `2:unionCases:` 屬 legacy migration / historical first-slice detail；RFC-PTCS-DYNAMIC-0003 修訂後的新 canonical key 為 direct DSL target `[ actorAddress; formDslId ]` 或 DU/template target `[ actorAddress; duTypeOrTemplateKey; canonicalArgString ]`。E2E 曾驗證 reload/readback key list、full key history、built-in add-key fallback，以及 duplicate target key 重送後 projection/card 維持單一項。Remaining：production split-service 後再補跨 process key registry replay；vNext 實作由 DYN-WBS-507/508/511 接手。 |
| DYN-WBS-406 | Cross-project RN proxy E2E | Review | 95 | DYN-T-406 / PTC3-T-067 | `G:\PulseTrade2.fs\Libs\PulseTrade.Comm.Spa\Scripts\verify.dynamicArguFormDurableProxy.playwright.fsx` 已完成並重跑 browser/runtime E2E：`--pcsl-root` + run-scoped fresh PCSL root、Dynamic form、`actor-dynamic` page creation、Dynamic add-key target、variable-length union-case key tail、PTCS ordered key readback、text/number/enum/tuple/bool/list controls、raw Argu args、`ActorArguTargetCommand.RawArgu` -> PTC RN DurableProxy low-cognitive path -> fCell2 string -> legacy echo actor -> `ActorArguTargetReply` -> PTCS full target-key history readback。Remaining：split-service RN.Host / ProcSupervisor / ShardingDelivery restart-redelivery / production provider proof 是 PTC RN/OPS 外部相依，不是 Dynamic UI E2E script 缺口。 |
| DYN-WBS-501 | Unified SDUI / Form DSL RFC flow | Done | 100 | DYN-T-501 | `RFC-PTCS-DYNAMIC-0003.unified-sdui-form-dsl-roadmap.md` 已把產品方向修正為 DSL-first；2026-06-26 再修正為 arg-string-driven backend DSL resolution：canonical DU target key 為 `[ actorAddress; duTypeOrTemplateKey; canonicalArgString ]`。`REQ.md`、`SA.md`、`SD.md`、`SDUI_DSL_zh-Hant.md`、`TEST.md`、`Traceability.md` 已同步。 |
| DYN-WBS-502 | Common DSL document model | Review | 90 | DYN-T-501 | `SduiFormDocument`、`SduiFormNode`、`SduiFormAction`、`SduiFormBinding`、`DynamicArguMetadata` 與 JSON codec 已在 package source 建立；DYN-T-501 以 PFCF_AKKA_CMD fixture 驗證 FormInput document、nodes/actions/bindings 與 tuple/list/enum/bool metadata。Remaining：Canvas renderer 仍沿用既有 `fskynet-sdui` path，後續可合併成同一 renderer abstraction。 |
| DYN-WBS-503 | Argu-to-FormDsl adapter | Review | 92 | DYN-T-502 | `ArguFormSchema.fromArgParserTemplateType` + `SduiFormDocument.fromArguFormSchema` 已形成 DU/`IArgParserTemplate` -> Form DSL adapter；DYN-T-502 驗證 PFCF_AKKA_CMD 多組 union cases 的 server codec 與 frontend `ClientRawArguCodec` 產生一致 raw arg string。Remaining：Host-local allowlist/unsupported type diagnostics 待 PTCS.Host slice。 |
| DYN-WBS-504 | Dynamic target resolver and add-key UX | Superseded / Review | 70 | DYN-T-503 | 已完成的 `[ actorAddress; formDslId ]` 與 `[ actorAddress; duTypeName; case1; ... ]` resolver 是 first-slice proof；RFC-PTCS-DYNAMIC-0003 修訂後，DU target 新 canonical 為 `[ actorAddress; duTypeOrTemplateKey; canonicalArgString ]`。Remaining 由 DYN-WBS-507/508/510 接手。 |
| DYN-WBS-505 | FormInput renderer v2 | Review | 97 | DYN-T-504 / DYN-T-506 / DYN-T-511 / DYN-T-512 | Renderer 已改以 Dynamic metadata 的 Form DSL document/schema registry 決定 form input；PFCF fixture 覆蓋 text/number/enum/tuple/bool/list raw arg codec。2026-06-26 beta8 修正 add-target DU/template key 為 editable text input + datalist，並驗證 partial canonical arg string `--pfcfedx trivial --pfcfgtcconf OIInf TAIFEX` 只渲染 parsed cases `PFCFEDX/PFCFGTCCONF`，不再回 full schema。PTCS.Host loopback Playwright gate 已驗證 full-form preview/send/echo、no-target cleanup、generic `actor-argu` add-target、canonical input 不被污染成 `"s"`。Remaining：query/stream option source、validation state、post-login public 81 human OAuth browser confirmation。 |
| DYN-WBS-506 | PTCS.Host demo DU and E2E gates | Review | 94 | DYN-T-505 / DYN-T-506 / DYN-T-512 | PTCS.Host 已使用 `C:\Users\Administrator\test_gemini\PulseTrade.Comm.Spa.Dynamic\doc\example DU.txt` 建 host-local PFCF demo subset，並以 loopback Playwright gate 驗證 actor key bound to DU/template + canonical arg string 可 render FormInput、submit exact raw command、echo readback。2026-06-27 beta12 gate 追加 user-facing/backend DSL UX regression：DU/template key 是自由 text input、不可用 select/datalist lock-in；`PFCFGTCCONF` 這類 Argu `'T list` 以可編輯 textbox row 呈現，Add value 產生空 textbox，每列 Remove 在 textbox 左側；backend list item schema 是 `text` 且不帶 enum options，不使用 enum dropdown 綁死 parser default。PTCS core action pool/tab close/`+ Page` 與 logical page type label/badge 由 PTCS beta20 負責，Dynamic beta12 只 owns `Bind target`、FormInput/list row renderer 與 `actor-dynamic` manifest badge `ad`。NuGet `#r` live host 已以 PTCS beta20 + Dynamic beta12 啟動並印 URL/actor/template/web-port/cluster-port/probe；beta12 push accepted, follow-up flat-container lookup lists `PulseTrade.Comm.Spa.Dynamic 0.1.3-beta12`。PTC 81 service 已 redeploy to beta20/beta12。Remaining：direct DSL target browser gate、post-login public 81 human visual confirmation。 |
| DYN-WBS-507 | Arg-string target RFC realignment | Done | 100 | DYN-T-507 | 2026-06-26 本輪修訂 RFC-PTCS-DYNAMIC-0003 與 stock docs：DU/template target key 改為 `[ actorAddress; duTypeOrTemplateKey; canonicalArgString ]`；沒有 `hub.useDynamicSdui(...)` 時 PTCS core 只取 actor address。 |
| DYN-WBS-508 | Backend target arg-string parser / Form DSL resolver | Done | 100 | DYN-T-507 / DYN-T-509 / DYN-T-511 / DYN-T-512 | `DynamicArgStringTarget` 已支援 `[ actorAddress; duTypeOrTemplateKey; canonicalArgString ]` parser-backed target、controlled parse failure、parsed root cases、root-section order 與 FormInput DSL `DefaultValues` projection；`DynamicArguResolveEndpoint` 已提供 `/client-extensions/dynamic/argu/resolve-target` JSON handler。Expecto 15/15 passed，新增 partial raw 只產生 parsed cases 的 regression gate。 |
| DYN-WBS-509 | Alias binding registry | Done | 100 | DYN-T-508 | `DynamicArguAliasBinding` / `DynamicFormDsl.fromArguFormSchemaWithAliases` 已支援 case/field/option alias；中文 label 只進 DSL display，canonical option value 與 raw Argu command 不變。 |
| DYN-WBS-510 | ParseResults / subcommand command builder | Done | 100 | DYN-T-510 | `DynamicArgStringTarget.scan` / `buildRawArgu` 已支援 `ParseResults<PFCF_AKKA_CMD_DATA_RANGE>` tail subcommand group；驗證 `datarange` token 在 root args 後、subcommand args 前，raw command exact match。 |
| DYN-WBS-511 | Arg-string backend DSL package gate | Done | 100 | DYN-T-511 | Package gate：backend `DynamicArguResolveEndpoint` parses `[ actorAddress; duTypeOrTemplateKey; canonicalArgString ]` with registered Argu parser and returns FormInput DSL with aliases/defaults；client append renderer compiles against the backend-resolved DSL fetch path；package test verifies full-form reconstruction of the PFCF `datarange` command, including list-inline tokens、root tuple defaults、tail tuple defaults、canonical enum default options and tail ordering。PTCS.Host browser echo gate is tracked by DYN-WBS-512。 |
| DYN-WBS-512 | Arg-string backend DSL Playwright E2E | Review | 99 | DYN-T-512 | `G:\PulseTrade.fs\Libs\PulseTrade.Comm\scripts\verify-ptcs-host-dynamic-argu-live.fsx` 已啟動 PTCS.Host loopback + Dynamic extension DLL，驗證 backend parse canonical arg string -> receive/render FormInput DSL with aliases/defaults -> submit -> raw Argu string equals expected PFCF data-range command -> DurableProxy echo readback。2026-06-27 beta12 verifier 覆蓋 manual add-target path、free editable DU/template key、no select/datalist type lock-in、partial raw 只渲染 `PFCFEDX/PFCFGTCCONF`、`PFCFGTCCONF` list defaults as editable textboxes with Add value/Remove-left、backend list item schema `text/options=0`、remove target/page 後重新加入不污染 canonical string、PTCS beta20 action pool/tab close/`+ Page`、logical `Actor Dynamic`/`Actor Argu` label + `ad`/`aa` badge、re-created `actor-dynamic` page Canonical Argu string visibility，與 Dynamic `Bind target` label。Public 81 service beta20/beta12 redeploy tracked by PTC verification；Remaining：post-login public 81 human OAuth browser confirmation、direct DSL target browser gate。 |
| DYN-WBS-513 | NuGet beta13 dependency rollout and live-host readback gate | Review | 96 | DYN-T-513 | Dynamic package advanced to `0.1.3-beta13` so package metadata and `#r` consumer gates align with PTCS `0.2.5-beta21` and `FAkka.WebSocket 1.569.101.301-win1`。Package tests still pass 15/15；PTC `verify-ptcs-dynamic-nuget-bundle.fsx` confirms beta13 bundle entries/JS markers；PTC `run-ptcs-dynamic-nuget-live-host.fsx -- --no-wait` confirms health、HTTP actor-argu send、WebSocket `actor-argu` send、state readback echo reply。NuGet push returned `Created` and follow-up flat-container lookup lists `PulseTrade.Comm.Spa.Dynamic 0.1.3-beta13`。Remaining：post-login public 81 human confirmation、direct DSL target browser gate、production split-service RN.Host provider gates。 |
| DYN-WBS-514 | Ordered target-key submit compatibility | Review | 98 | DYN-T-514 / PTC-VFY-004 | Dynamic package advanced to `0.1.3-beta14` for PTCS beta23 ordered append-page key compatibility. Renderer submit payload now includes `keyJson`; append renderer normalizes legacy sorted triples where old PCSL exposed `[raw; actor; template]` back into canonical `[actor; template; raw]` before resolve/render/submit. PTCS core remains owner of stream key canonicalization and stale demo key cleanup; Dynamic only repairs client payload/order. Verification：Release build passed with existing WS9002/NU5123/missing-readme warnings；PTC `verify-ptcs-host-dynamic-argu-live.fsx` injects a legacy sorted bucket and confirms selected key JSON order、Send echo、canvas；PTC bundle/live-host gates passed for PTCS beta23 + Dynamic beta14。NuGet push returned `Created` / `Your package was pushed`; immediate v3 lookup at 2026-06-27 18:37 +08:00 was propagation-pending, and follow-up flat-container lookup at 2026-06-27 18:47 +08:00 lists beta14。Remaining：post-login public 81 human confirmation、direct DSL target browser gate、production split-service RN.Host provider gates。 |
| DYN-WBS-515 | Add-target alias and collapsed-panel UX | Review | 98 | DYN-T-515 / PTC-VFY-004 | Dynamic package advanced to `0.1.3-beta15` for PTCS beta24 target alias UI. Add-key renderer now submits `{ keys; displayName }`, renders actor address + DU/template + target alias + canonical Argu string, uses Clean/OK instead of `Bind target`, and relies on PTCS core to collapse the add panel after successful OK. Target tuple remains `[actorAddress; duTypeOrTemplateKey; canonicalArgString]`; alias is display-only metadata owned by PTCS append-page key registry. Verification：Release build passed with existing WS9002/NU5123/missing-readme warnings and package tests `15/15`；PTC browser verifier passed alias input/list display、OK label、post-submit collapse、canonical input width、echo、canvas；PTC bundle/live-host gates passed and confirmed exact beta24/beta15 assembly paths。NuGet push returned `Created` / `Your package was pushed`; follow-up flat-container lookup lists beta15。PTC public 81 redeployed to `live81-ptcs-beta24-dynamic-beta15-alias-202606272001`。Remaining：post-login public 81 human confirmation、direct DSL target browser gate、production split-service RN.Host provider gates。 |
| DYN-WBS-516 | Add-target Cancel action | Review | 98 | DYN-T-516 / PTC-VFY-004 | Dynamic package advanced to `0.1.3-beta16` for PTCS beta25 Cancel action support. Add-key renderer now renders `Clean / Cancel / OK`; Cancel calls PTCS core `cancelKey` and only collapses the Add target panel, without submitting a target or clearing existing keys. Target tuple remains `[actorAddress; duTypeOrTemplateKey; canonicalArgString]`; alias stays display-only metadata. Verification：Release build passed with existing WS9002/NU5123/missing-readme warnings and package tests `15/15`；PTC browser verifier passed Cancel label、Cancel-time panel collapse、alias/canonical width、echo、canvas；PTC bundle/live-host gates passed and confirmed exact beta25/beta16 assembly paths。NuGet push returned `Created` / `Your package was pushed`; flat-container lookup at 2026-06-27 21:15 +08:00 remained propagation-pending for beta16。PTC public 81 redeployed to `live81-ptcs-beta25-dynamic-beta16-cancel-202606272112`。Remaining：post-login public 81 human confirmation、direct DSL target browser gate、production split-service RN.Host provider gates。 |
| DYN-WBS-517 | Canvas Tree renderer for ActorTreeDocument | Superseded / Planned | 0 | DYN-T-517 / PTCS-T-054D | Original plan reserved Canvas `Tree` node semantics for PTCS Actors tab. After `RFC-PTC-SPA-0010` / `RFC-PTCS-DYNAMIC-0005`, this is superseded by page-level `ActorsPage` rendering: Dynamic must render the whole `/actors` page, not only a tree widget or Canvas preview. |
| DYN-WBS-518 | Actor Dynamic action modes and proxy key builder | Review | 94 | DYN-T-520..525 / PTCS-T-059A..F | `RFC-PTCS-DYNAMIC-0004.actor-dynamic-action-modes.md` accepted and implemented in `PulseTrade.Comm.Spa.Dynamic 0.1.3-beta22`. Add-key renderer claims mode-aware shapes `actor-dynamic-target` / `actor-dynamic-proxy` / `actor-argu-target`; Actor Argu remains FormInput-only and exposes no proxy/canvas behavior; Actor Dynamic direct actor key returns to PTCS fallback arbitrary input/canvas reply path; proxy key builder stores `[proxyActorAddress; "proxy-v1"; rnActorAddress; targetKind]` and uses PTCS fallback textarea for payload. Verification passed: Dynamic package tests `15/15`, PTC bundle gate for PTCS beta37/Dynamic beta22, PTC live Playwright verifier covering direct actor, target, proxy, canvas, Actor Argu no-proxy, Actors full-address gates, and NuGet flat-container indexing for Dynamic beta22. Remaining: production split-service RN.Host proxy proof and Dynamic Canvas Tree renderer follow-up. |
| DYN-WBS-519 | ActorsPage page-level renderer RFC and implementation | Review | 99 | DYN-T-526..532 / PTCS-T-060A..H | `RFC-PTCS-DYNAMIC-0005.actors-page-renderer.md` added. Current slices register a dedicated `ActorsPage` page renderer from existing `Client/ActorDynamicTab.fs`, keep generic Canvas message renderer separate, and render a page-level Dynamic Actors UI with action shell, count cards, host/port node blocks, hierarchy rows, grid rows, and boxed `+` / `-` tree toggles when PTCS sends an `ActorTopologyPage` document. Verification: Dynamic tests passed 18/18 with `WebSharperRunCompiler=false`; normal Release WebSharper bundle build passed after stopping stale `wsfscservice`; PTCS source-host F# Playwright gate passed with Dynamic host present, fallback rows `0`, core `actor-node` / `actor-card` rows `0`, blocks ordered PTCS Host -> GW Host -> RN Host, full actor addresses visible, virtual `/user` and `/system` ancestors rendered inside their owning host blocks, status dots/connectors present, and toggle click changing `- / true` -> `+ / false` with rows restored after re-expand. `G:\PulseTrade2.fs\Libs\PulseTrade.Comm.Spa\Scripts\verify.actorsPageDynamic.playwright.fsx` repeats that accepted path as a reusable F# Playwright locator gate. Package rollout advanced through `PulseTrade.Comm.Spa.Dynamic 0.1.3-beta56`, paired with PTCS `0.2.5-beta66`; PTC bundle verifier target is exact beta66/beta56 assembly/package paths while preserving the beta43/beta33 ActorsPage marker coverage. Current formal public 81 / loopback 82 release is `live81-82-ptcs-beta66-dynamic-beta56-production-sql-private-lan-202607011125`, verified by PTC deployment alignment and health; earlier `/actors` Dynamic renderer and `/page/assterry` FormInput send/reply evidence remains `G:\PulseTrade.fs\log\20260701\ptcs81-beta66-actors-snapshot.md`, `G:\PulseTrade.fs\log\20260701\ptcs81-beta66-actors.png`, and `G:\PulseTrade.fs\log\20260701\ptcs81-beta66-actors-console.txt`. Constraint: WebSharper 10.1.5.674 requires stale helper cleanup; current classifier still uses one `IndexOf("ActorTopologyPage")` token. Remaining：strict schema parser, server-side persisted report schedule, IndexedDB restart/cache sync, cross-service GW/RN registry feed, includeOffline diagnostics UI, and failover visual states. |
| DYN-WBS-520 | PTCS dependency consumes NuGet package only | Done | 100 | DYN-VFY-001 / PTC-VFY-007 / DYN-VFY-008 | 2026-06-29 `src/PulseTrade.Comm.Spa.Dynamic.fsproj` removed the direct ProjectReference to `G:\PulseTrade2.fs\Libs\PulseTrade.Comm.Spa\PulseTrade.Comm.Spa.fsproj`. Current pin is `PulseTrade.Comm.Spa [0.2.5-beta66]` in `PulseTrade.Comm.Spa.Dynamic 0.1.3-beta56`. Verification target: normal Release WebSharper build/pack, Dynamic tests `18/18`, beta56 nupkg copied to SDK `10.0.301` `FSharp\library-packs`, and PTC bundle verifier with exact beta66/beta56 paths. 2026-07-01 `src\poc.full.nuget.journal.ACL.fsx` gained `--if-dyna-port` and production-sql encrypted connection args; demo and production-sql no-wait gates passed with dynamic ports, including WZ/sys-admin vs Terry黑粉 SQL ACL difference proof and disabled-terry rejection. |
| DYN-WBS-521 | ACL2 final open-extension boundary POC | Review | 96 | DYN-VFY-009 / PTCS-ACL-T-014 / PTCS-LOGIN-T-011 | First slice added `src\PulseTrade.Comm.Spa.ACL` and `src\PulseTrade.Comm.Spa.Login` open package projects plus `src\poc.full.nuget.journal.ACL2.fsx`. Current open-provider slice uses PTCS beta70 / Dynamic beta60 / Spa.ACL alpha10 / Spa.Login alpha12. Release build/pack passed for Spa.Login alpha12, copied nupkg to SDK `10.0.301` `FSharp\library-packs`, and NuGet push returned Created. `useAcl` registers PTCS client-extension manifest, package `contentFiles` script asset, WebSharper runtime dependency asset, ACL snapshot observer, and capability provider. `usePtcsLogin` now uses the open package's own `PtcsLogin.provider` through `Server.withBrowserAuth`, owns the local-login route set/cookie/session adapter, and registers the Login extension manifest/script/runtime/renderer. ACL2 dynamic-port demo no-wait passed; ACL2 production-SQL no-wait remains covered by the alpha3 encrypted SQL proof. PTCS browser Playwright passed through exact beta70/beta60 + Spa.ACL alpha10 + Spa.Login alpha12 packages and asserts extension manifest/script URL load plus active Login renderer / ACL observer / capability provider. Formal service release `live81-82-ptcs-beta70-dynamic-beta60-open-acl-login-assetfix-202607021416` passed public 81 OAuth redirect, loopback 82 SQL local login, `/acl/api/snapshot`, and direct Spa.ACL/Spa.Login script marker fetches. Remaining: transitional PTCS core fallback cleanup. |

## 2026-06-29 Current ActorsPage / PingPong Stop Update

- `DYN-WBS-519` current package slice is `PulseTrade.Comm.Spa.Dynamic 0.1.3-beta56`, paired with PTCS `0.2.5-beta66`.
- Dynamic beta56 is the current alignment target after PTCS beta66 protected ACL policy reload endpoint.
- `src\poc.full.nuget.journal.ACL.fsx -- --no-wait --local-port 18102 --github-port 18101 --cluster-port 18801 --pcsl-root .\.pcsl\verify.acl.beta56.dual-host.quiet` is the fixed-port package-aligned ACL/Login POC command; dynamic-port demo and production-sql proof also passed on 2026-07-01 using `--if-dyna-port`. Latest production-sql proof used encrypted file `D:\ingted.com\ptcs-sql-connection.enc.txt`, ACL table `AclPolicySnapshotPoc`, and PCSL root `G:\PulseTrade.fs.Comm.Log\manual\ptcsDynamicNugetJournalAcl\pcsl_wz_terry_20260701_01`; stdout reports WZ/sys-admin full rights, Terry黑粉 add-target denied but remove/send allowed on AssTerry, send denied on DamnWZ, and disabled-terry login 401.
- `src\poc.full.nuget.journal.fsx -- --no-wait` verifies ActorRegistry stop/reload behavior: PingPong is projected as `terminated`, active ActorTree DSL filters it, and `includeOffline` keeps the stopped row only for diagnostics.
- Formal service deployment is `live81-82-ptcs-beta66-dynamic-beta56-production-sql-private-lan-202607011125`, confirmed by PTC deployment alignment, loopback 81/82/8798 `/healthz`, public `https://my-ai.co.in:81/healthz`, and 82 SQL PTCS.Login `/acl/api/snapshot` positive path. Earlier Playwright MCP `/actors` evidence and `/page/assterry` FormInput send/reply evidence remain valid for beta66/beta56 Dynamic renderer behavior.
- `src\poc.full.nuget.journal.ACL.fsx` is now transitional runtime behavior proof. Open-extension boundary proof now includes `src\poc.full.nuget.journal.ACL2.fsx -- --if-dyna-port --no-wait --demo`, the production-SQL no-wait command recorded in `DYN-VFY-009`, PTCS browser Playwright with exact beta70/beta60 + Spa.ACL/Login alpha10/alpha12 packages plus extension manifest/script and active renderer/observer/provider assertions, and formal public 81/loopback 82 service proof on the beta70 assetfix release. Remaining final gate stays under DYN-WBS-521 as fallback cleanup.

## Dynamic Argu Form 相依順序

1. `DYN-WBS-401..406` 是 Argu-first first slice，保留作為已完成探索與 regression 來源。
2. `DYN-WBS-501` 先完成 RFC-0003 DSL-first 文件 review。
3. PTCS `WBS-053A..F` 提供 unified target extension contract；Dynamic `DYN-WBS-502/503` 可並行開發 pure package tests。
4. `DYN-WBS-507` 已修正 RFC-0003 target design。
5. `DYN-WBS-508/509/510` 已完成 package/backend parser、alias、subcommand raw builder。
6. `DYN-WBS-505/511` 再把 renderer 改成吃 backend-resolved FormInput DSL，並以 Playwright 驗證 arg-string path。
7. `DYN-WBS-506` 與 PTCS.Host demo 串接 `example DU.txt`，先完成 direct DSL target 與 DU/template arg-string target E2E。
8. PTC RN `PTC3-063/066/065` 完成 controller-region restart redelivery、provider-bound completion 與 service-window proof 後，再把 `DYN-WBS-506/511` / `PTC3-067` 從 browser/runtime E2E 推進到 production split-service E2E。
9. `DYN-WBS-518` 先修復 Actor Dynamic/Actor Argu action mode 邊界。
10. `DYN-WBS-519` 取代 `DYN-WBS-517` 作為 Actors tab 後續主線：改做 page-level `ActorsPage` renderer，而不是 generic Canvas Tree preview；proxy key 的 production RN Host split-service E2E 仍依 PTC RN/RN.Host WBS 完成度推進。
