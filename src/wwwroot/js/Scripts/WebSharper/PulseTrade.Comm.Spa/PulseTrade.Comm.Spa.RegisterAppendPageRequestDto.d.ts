export function New(pageId, title, setName, shape, tabId, tabMode, path, description)
export default interface RegisterAppendPageRequestDto {
  pageId:string;
  title:string;
  setName:string;
  shape:string;
  tabId:string;
  tabMode:string;
  path:string;
  description:string;
}
