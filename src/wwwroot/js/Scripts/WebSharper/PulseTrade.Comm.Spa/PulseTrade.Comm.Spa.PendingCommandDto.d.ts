export function New(commandId, serverRealityId, kind, target, url, method, payloadJson, status)
export default interface PendingCommandDto {
  commandId:string;
  serverRealityId:string;
  kind:string;
  target:string;
  url:string;
  method:string;
  payloadJson:string;
  status:string;
}
