export function New(streamId, newestSequence, cachedCount, source, touchedAt)
export default interface BrowserWatermarkDto {
  streamId:string;
  newestSequence:string;
  cachedCount:number;
  source:string;
  touchedAt:string;
}
