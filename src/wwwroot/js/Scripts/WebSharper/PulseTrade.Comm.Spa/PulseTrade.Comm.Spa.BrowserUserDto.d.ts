export function New(participantId, displayName, login, authenticated)
export default interface BrowserUserDto {
  participantId:string;
  displayName:string;
  login:string;
  authenticated:boolean;
}
