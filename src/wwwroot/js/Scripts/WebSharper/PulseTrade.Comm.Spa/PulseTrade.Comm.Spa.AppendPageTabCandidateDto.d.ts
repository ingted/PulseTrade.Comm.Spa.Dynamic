export function New(pageId, tabId, title, setName, shape, path, visible, registeredAtUtc, hiddenAtUtc)
export default interface AppendPageTabCandidateDto {
  pageId:string;
  tabId:string;
  title:string;
  setName:string;
  shape:string;
  path:string;
  visible:boolean;
  registeredAtUtc:string;
  hiddenAtUtc:string;
}
