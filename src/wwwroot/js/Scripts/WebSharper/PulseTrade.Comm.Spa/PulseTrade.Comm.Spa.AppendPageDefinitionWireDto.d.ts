export function New(schema, pageId, tabId, path, title, setName, shape, description, keyPlaceholder, valuePlaceholder, defaultKey, tags)
export default interface AppendPageDefinitionWireDto {
  schema:string;
  pageId:string;
  tabId:string;
  path:string;
  title:string;
  setName:string;
  shape:string;
  description:string;
  keyPlaceholder:string;
  valuePlaceholder:string;
  defaultKey:string;
  tags:string[];
}
