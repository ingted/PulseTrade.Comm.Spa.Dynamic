export function New(pageId, keyJson, valueText, direction, tags)
export default interface AppendPageAppendRequestDto {
  pageId:string;
  keyJson:string;
  valueText:string;
  direction:string;
  tags:string[];
}
